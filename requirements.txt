fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
python-multipart>=0.0.12
pydantic>=2.9
python-dotenv>=1.0

yfinance>=0.2.50
pandas>=2.2
numpy>=1.26

# Reads the XTB broker export. Used directly rather than through
# pandas.read_excel: the sheets carry irregular preambles, a summary block and
# nested row groups under a single header, so cell-level access beats trying to
# coerce them into a rectangular DataFrame.
openpyxl>=3.1

anthropic>=0.40
openai>=1.55

# Renders the model's Markdown answer. Declared explicitly rather than relied
# on as a transitive dependency of the provider SDKs. CommonMark with
# html=False, so raw HTML in model output is escaped instead of executed.
markdown-it-py>=3.0
