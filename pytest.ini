; Standalone config rather than a [tool.pytest] table: the project has no
; pyproject.toml, and adding one just to hold four lines would imply a
; packaging story that does not exist here.
[pytest]
pythonpath = .
testpaths = tests
addopts = -q
